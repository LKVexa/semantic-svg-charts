# License scope

Original code and September 2026 modifications by RUSSELL PHILIP SMITHSON are
provided under Apache 2.0; see LICENSE and NOTICE. Python and build tools are
not bundled and retain their own licenses.

# Third-party notices — JY-S027-P001

No third-party code vendored; stdlib-only deterministic renderer.

## Negative retrieval results (for the ledger)
- No deterministic-SVG chart-engine donor found in the yard for this
  zero-dependency fit (searched: svg chart, visualization engine,
  deterministic render) — `build-new: spec validator, deterministic SVG
  renderer, semantics block, clipping inspector (searched: svg chart,
  visualization engine)`.
